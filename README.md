# Konspekt

Умный блокнот для встреч: локальный аналог Granola с упором на русский
язык (GigaAM) и приватность. Окно поверх остальных, живёт в трее.

## Запуск

```bash
uv sync
uv run python -m app
```

## Горячие клавиши

| Комбинация | Действие |
|---|---|
| `Ctrl+Shift+K` | Показать / скрыть окно (глобально) |
| `Ctrl+N` | Новая встреча |
| `Ctrl+R` | Начать / остановить запись |
| `Ctrl+S` | Сохранить заметки |

Закрытие окна прячет его в трей, а не завершает приложение.
Выход — через меню трея.

## Где лежат данные

```
%APPDATA%\Konspekt\
  konspekt.db       встречи, заметки, транскрипты
  settings.json     настройки и геометрия окна
  konspekt.log      лог
  audio/            записи (этап 2)
  models/           веса ASR (этап 3)
```

## Статус

Этап 1 готов: окно, трей, хранилище, редактор заметок.
Запись звука и распознавание — заглушки с настоящими интерфейсами.
Подробности и план — в [ROADMAP.md](ROADMAP.md).

## Проверка

```bash
uv run python smoke_test.py
```
